/* Turn on cross-device sync: paste your Google "client ID" between the quotes, then save this file.
   The README ("Turn on sync") shows how to get one, for free, in about ten minutes.
   Leave it empty and the planner works exactly as before (gardens stay in each browser). */
window.TGP_CONFIG = { googleClientId: '' };
